<%= simple_form_for([:builder, current_site, current_setting]) do |f| %>
  <div class="half-row">
    <%= f.input :title %>
    <%= f.input :slug, :label => "Method Name" %>
  </div>
  <%= f.input :body, :label => 'Value' %>
  <%= f.submit "Save Setting" %>
<% end %>
