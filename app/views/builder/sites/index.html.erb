<%= builder_page_title('My Sites') do %>
  <%= content_tag(
    :div,
    link_to('New Site',  new_builder_site_path, :class => 'new'),
    :class => 'new-buttons dropdown'
  ) %>
<% end %>

<%= content_tag(:section, data_table(my_sites, 'site'), :id => 'page-content') %>
