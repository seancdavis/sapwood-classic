<%= current_template_breadcrumbs %>

<%= builder_page_title("Edit #{current_template_group.title} Section") do %>
  <%= subtitle("Edit the name or position of the form section.") %>
<% end %>

<section id="page-content">
  <%= render 'form' %>
</section>

<%= sidebar('builder/templates/sidebar') %>
