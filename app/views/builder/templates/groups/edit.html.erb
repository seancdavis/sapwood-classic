<%= current_template_breadcrumbs %>

<%= builder_page_title("Edit #{current_template_group.title} Group") do %>
  <%= subtitle("Edit the name or position of the form group.") %>
<% end %>

<section id="page-content">
  <%= render 'form' %>
</section>

<%= sidebar('builder/templates/sidebar') %>
