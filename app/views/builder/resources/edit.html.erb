<%= builder_page_title(
  current_resource_breadcrumbs,
  :class => 'breadcrumbs no-border'
) do %>
  <%= render :partial => 'tabs' %>
<% end %>

<section id="page-content" class="full-width templates">
  <%= render 'form' %>
</section>
