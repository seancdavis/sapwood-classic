<%= builder_page_title("Edit #{current_page_type.title}") do %>
  <%= link_to(
    'New Page Type', 
    builder_route([current_page_type], :new), 
    :class => 'new' 
  ) %>
  <%= link_to(
    "New (Root) #{current_page_type.label}",
    new_builder_site_page_path(current_site, :page_type => current_page_type.slug),
    :class => 'new'
  ) %>
<% end %>

<%= sidebar %>

<%= content_tag(:section, render('form'), :id => 'page-content') %>
