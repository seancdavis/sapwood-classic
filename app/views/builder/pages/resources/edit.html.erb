<%= builder_page_title(
  current_page_breadcrumbs,
  :class => 'breadcrumbs no-border'
) do %>
  <%= render :partial => 'builder/pages/tabs' %>
<% end %>

<section class="form full-width" id="page-content">
  <%= render 'form' %>
</section>
