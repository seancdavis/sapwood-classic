<%= builder_page_title(
  menus_breadcrumbs,
  :class => 'breadcrumbs no-border'
) do %>
  <%= render :partial => 'builder/menus/tabs' %>
<% end %>

<section id="page-content" class="data-container">
  <%= render 'form' %>
</section>
